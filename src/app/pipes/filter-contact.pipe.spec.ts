import { FilterContactPipe } from './filter-contact.pipe';

describe('FilterContactPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterContactPipe();
    expect(pipe).toBeTruthy();
  });
});
